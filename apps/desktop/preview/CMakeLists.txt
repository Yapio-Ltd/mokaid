add_library(mokaid_preview STATIC preview_controller.cpp document_format.cpp
    include/mokaid/preview/preview_controller.hpp include/mokaid/preview/resource_policy.hpp
    include/mokaid/preview/document_format.hpp include/mokaid/preview/native_file_preview.hpp)
target_include_directories(mokaid_preview PUBLIC include)
target_link_libraries(mokaid_preview PUBLIC Qt6::Core Qt6::WebEngineQuick mokaid_application PRIVATE Qt6::Gui Qt6::Concurrent mokaid_options)
if(APPLE)
  target_sources(mokaid_preview PRIVATE native_file_preview.mm)
  set_source_files_properties(native_file_preview.mm PROPERTIES COMPILE_FLAGS "-fobjc-arc")
  target_link_libraries(mokaid_preview PRIVATE "-framework AppKit" "-framework Quartz")
else()
  target_sources(mokaid_preview PRIVATE native_file_preview_stub.cpp)
endif()
if(BUILD_TESTING)
  qt_add_executable(mokaid_preview_policy_tests tests/resource_policy_tests.cpp)
  target_include_directories(mokaid_preview_policy_tests PRIVATE include)
  target_link_libraries(mokaid_preview_policy_tests PRIVATE Qt6::Core Qt6::Test)
  add_test(NAME preview_resource_policy COMMAND mokaid_preview_policy_tests)
  qt_add_executable(mokaid_preview_navigation_tests tests/navigation_tests.cpp)
  target_link_libraries(mokaid_preview_navigation_tests PRIVATE mokaid_preview Qt6::QuickControls2 Qt6::Test mokaid_options)
  target_compile_definitions(mokaid_preview_navigation_tests PRIVATE MOKAID_PREVIEW_QML_DIRECTORY="${CMAKE_CURRENT_SOURCE_DIR}/../presentation/qml")
  add_test(NAME desktop-preview-navigation COMMAND mokaid_preview_navigation_tests)
  qt_add_executable(mokaid_preview_format_tests tests/document_format_tests.cpp document_format.cpp)
  target_include_directories(mokaid_preview_format_tests PRIVATE include)
  target_link_libraries(mokaid_preview_format_tests PRIVATE Qt6::Gui Qt6::Test mokaid_options)
  add_test(NAME desktop-preview-formats COMMAND mokaid_preview_format_tests)
endif()
