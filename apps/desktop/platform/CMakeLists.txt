add_library(mokaid_platform STATIC credential_store.cpp)
target_include_directories(mokaid_platform PUBLIC include)
target_link_libraries(mokaid_platform PUBLIC Qt6::Core PRIVATE mokaid_options)
if(APPLE)
  target_link_libraries(mokaid_platform PRIVATE "-framework Security" "-framework CoreFoundation")
elseif(WIN32)
  target_link_libraries(mokaid_platform PRIVATE Advapi32)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/updates/CMakeLists.txt")
  add_subdirectory(updates)
endif()
