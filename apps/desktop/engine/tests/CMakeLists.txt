add_executable(mokaid_engine_tests engine_tests.cpp)
target_link_libraries(mokaid_engine_tests PRIVATE mokaid_engine)
add_test(NAME desktop.engine COMMAND mokaid_engine_tests)
set_tests_properties(desktop.engine PROPERTIES TIMEOUT 30)
add_executable(mokaid_asset_format_tests asset_format_tests.cpp)
target_link_libraries(mokaid_asset_format_tests PRIVATE mokaid_engine)
add_test(NAME desktop.asset_format COMMAND mokaid_asset_format_tests)
set_tests_properties(desktop.asset_format PROPERTIES TIMEOUT 30)
add_executable(mokaid_navigation_tests navigation_tests.cpp)
target_link_libraries(mokaid_navigation_tests PRIVATE mokaid_engine)
add_test(NAME desktop.navigation COMMAND mokaid_navigation_tests)
set_tests_properties(desktop.navigation PROPERTIES TIMEOUT 30)
add_executable(mokaid_traffic_tests traffic_tests.cpp)
target_link_libraries(mokaid_traffic_tests PRIVATE mokaid_engine)
add_test(NAME desktop.traffic COMMAND mokaid_traffic_tests)
set_tests_properties(desktop.traffic PROPERTIES TIMEOUT 90)
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/../../build/assets/office.mokaidasset")
  add_executable(mokaid_office_activity_tests activity_tests.cpp)
  target_link_libraries(mokaid_office_activity_tests PRIVATE mokaid_engine)
  add_test(NAME desktop.office_activities COMMAND mokaid_office_activity_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../build/assets")
  set_tests_properties(desktop.office_activities PROPERTIES TIMEOUT 180)
  add_test(NAME desktop.real_assets COMMAND mokaid_engine_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../build/assets")
  set_tests_properties(desktop.real_assets PROPERTIES TIMEOUT 60)
  add_test(NAME desktop.real_navigation COMMAND mokaid_navigation_tests "${CMAKE_CURRENT_SOURCE_DIR}/../../build/assets")
  set_tests_properties(desktop.real_navigation PROPERTIES TIMEOUT 30)
endif()
